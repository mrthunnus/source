spring:
  application:
    name: RabbitmqPolicController
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8763

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org: ERROR